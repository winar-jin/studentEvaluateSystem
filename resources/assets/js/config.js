/**
 *
 * Created by jin on 2017/5/21.
 */

const USER_ID = localStorage.getItem('userid__main'), // 获取用户的ID
    USER_ROLE = localStorage.getItem('role__main'); // 获取用户的角色（admin，student，teacher）

export {USER_ID, USER_ROLE};
